% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update_meta_data.R
\name{update_meta_data}
\alias{update_meta_data}
\title{Aktualisiere Meta Ads Masterdatei inkrementell (Delta-Update)}
\usage{
update_meta_data(
  granularity = c("daily", "hourly"),
  end_date = Sys.Date(),
  overlap_days = 2,
  datadir = NULL,
  token = Sys.getenv("META_ACCESS_TOKEN"),
  ad_acc_id = Sys.getenv("META_AD_ACCOUNT_ID")
)
}
\arguments{
\item{granularity}{`"daily"` oder `"hourly"`.}

\item{end_date}{Enddatum der Aktualisierung (Default: `Sys.Date()`).}

\item{overlap_days}{Tage ab Master-Ende, die erneut gezogen und überschrieben
werden (Default 2).}

\item{datadir}{Basis-Datenordner. Default: `datadir` aus `.GlobalEnv`
(gesetzt via `~/workspace/local.R`).}
}
\value{
Unsichtbar: der aktualisierte Master-Datensatz als `tibble`.
}
\description{
Liest die Masterdatei (`meta_daily_request.rds` oder
`meta_hourly_request.rds`) aus `~/data/meta/`, ermittelt das höchste Datum
(aus `date_start` bzw. `date`) und zieht nur den fehlenden Zeitraum über
`get_meta_data()` nach. Die letzten `overlap_days` werden erneut geladen
und im Master überschrieben, da Meta Spend-Daten mit 24–48h Verzug
finalisiert.
}
\examples{
\dontrun{
update_meta_data("daily")
update_meta_data("hourly", overlap_days = 3)
}
}
\seealso{
Other adribute: 
\code{\link{get_meta_data}()}
}
\concept{adribute}
